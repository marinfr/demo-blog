= form_with(model: @post, html: { class: "js-form" }, local: true) do |f|
  .field
    = f.label :title
    = f.text_field :title
  .field
    = f.label :content
    = f.text_area :content, value: @post.content.try(:gsub, "\r\n<br>", "<br>")

  .actions
    = f.submit
